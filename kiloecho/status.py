# Increment 3 — localhost/terminal status view.
# Reads ke-runs.jsonl (and costs.jsonl from the cost addon) to show
# queue, current run, recent outcomes, and running cost totals.
# Wire to `ke status` in __main__.py once implemented.
